[project]
name = "fast-f1-app"
version = "0.2.0"
description = "Textual app for browsing current-season Formula 1 results with FastF1"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Zhihang Liu" }]
keywords = ["formula1", "f1", "fastf1", "motorsport", "textual", "tui"]
# No "License :: OSI Approved" classifier on purpose: core metadata 2.4 treats a
# licence classifier alongside the SPDX `license` expression above as an error,
# and PyPI rejects the upload.
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Terminals",
    "Topic :: Utilities",
]
dependencies = [
    "fastf1>=3.7.0",
    "plotext>=5.3.2",
    "textual>=0.89.1",
]

[project.urls]
Homepage = "https://github.com/ekss1121/fast-f1-app"
Repository = "https://github.com/ekss1121/fast-f1-app"
Issues = "https://github.com/ekss1121/fast-f1-app/issues"

[project.scripts]
fast-f1-app = "fast_f1_app:main"

[build-system]
# 1.26.0 is where hatchling learned core metadata 2.4 — the SPDX `license`
# expression and glob-style `license-files` above need it. An older backend
# resolved in a fresh build environment would fail on them.
requires = ["hatchling>=1.26.3"]
build-backend = "hatchling.build"

# Without this, hatchling sweeps the whole working tree into the sdist —
# editor settings, agent config and all. Ship only what builds and tests the
# app; hatchling adds .gitignore on top regardless.
[tool.hatch.build.targets.sdist]
include = [
    "fast_f1_app.py",
    "tests/",
    "README.md",
    "LICENSE",
    "pyproject.toml",
]
